{
    "tasks": [
        {
            "type": "shell",
            "label": "tasks",
            "command": "make",
            "args": [
                "debug"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/src"
            }
        }
    ],
    "version": "2.0.0"
}